{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start main",
            "type": "debugpy",
            "request": "launch",
            "module": "src.main",
            "cwd": "${workspaceFolder}"
        }
    ]
}